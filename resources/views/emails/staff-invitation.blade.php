<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml" xmlns:v="urn:schemas-microsoft-com:vml" xmlns:o="urn:schemas-microsoft-com:office:office">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="x-apple-disable-message-reformatting">
    <title>You're Invited — SKIPS Academic Tracker</title>
    <!--[if mso]>
    <noscript><xml><o:OfficeDocumentSettings><o:PixelsPerInch>96</o:PixelsPerInch></o:OfficeDocumentSettings></xml></noscript>
    <![endif]-->
    <style>
        /* Reset */
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body, table, td, a { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
        table, td { mso-table-lspace: 0pt; mso-table-rspace: 0pt; }
        img { -ms-interpolation-mode: bicubic; border: 0; line-height: 100%; outline: none; text-decoration: none; }
        table { border-collapse: collapse !important; }

        body {
            background-color: #f0f8fc;
            font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            margin: 0 !important;
            padding: 0 !important;
            width: 100% !important;
        }

        /* Wrapper */
        .email-wrapper {
            background-color: #f0f8fc;
            padding: 40px 16px;
        }

        /* Container */
        .email-container {
            max-width: 580px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 8px 32px rgba(0, 78, 111, 0.10);
        }

        /* Header */
        .email-header {
            background: #003750;
            padding: 36px 40px 32px;
            position: relative;
            text-align: left;
        }

        .header-logo-row {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 28px;
        }

        .header-logo-text {
            font-size: 13px;
            color: rgba(255,255,255,0.55);
            letter-spacing: 0.08em;
            text-transform: uppercase;
            font-weight: 600;
        }

        .header-badge {
            display: inline-block;
            background: #fbba00;
            color: #003750;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            padding: 4px 12px;
            border-radius: 99px;
            margin-bottom: 14px;
        }

        .header-title {
            font-size: 26px;
            font-weight: 700;
            color: #ffffff;
            line-height: 1.2;
            margin-bottom: 6px;
            font-family: Georgia, 'Times New Roman', serif;
        }

        .header-subtitle {
            font-size: 14px;
            color: rgba(255,255,255,0.55);
            line-height: 1.6;
        }

        /* Accent bar */
        .accent-bar {
            height: 4px;
            background: linear-gradient(90deg, #fbba00 0%, #d49e00 100%);
        }

        /* Body */
        .email-body {
            padding: 36px 40px;
        }

        .greeting {
            font-size: 16px;
            color: #003750;
            font-weight: 600;
            margin-bottom: 14px;
        }

        .body-text {
            font-size: 15px;
            color: #504f48;
            line-height: 1.75;
            margin-bottom: 20px;
        }

        /* Info card */
        .info-card {
            background: #f0f8fc;
            border: 1px solid #b5d4f4;
            border-radius: 10px;
            padding: 20px 22px;
            margin: 24px 0;
        }

        .info-card-title {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: #0a6a8f;
            margin-bottom: 14px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 7px 0;
            border-bottom: 1px solid #d4eef9;
            font-size: 13px;
        }

        .info-row:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .info-label {
            color: #0a6a8f;
            font-weight: 600;
            min-width: 110px;
        }

        .info-value {
            color: #252520;
            font-weight: 500;
            text-align: right;
        }

        /* CTA Button */
        .cta-wrap {
            text-align: center;
            padding: 8px 0 24px;
        }

        .cta-btn {
            display: inline-block;
            background: #fbba00;
            color: #003750 !important;
            font-size: 15px;
            font-weight: 700;
            text-decoration: none;
            padding: 15px 36px;
            border-radius: 10px;
            letter-spacing: 0.02em;
            box-shadow: 0 4px 16px rgba(251, 186, 0, 0.35);
        }

        .cta-expiry {
            font-size: 12px;
            color: #9c9b94;
            margin-top: 10px;
        }

        /* Divider */
        .divider {
            height: 1px;
            background: #eeede9;
            margin: 24px 0;
        }

        /* Features list */
        .features-list {
            list-style: none;
            padding: 0;
            margin: 0 0 24px;
        }

        .features-list li {
            display: flex;
            align-items: flex-start;
            gap: 10px;
            padding: 7px 0;
            font-size: 14px;
            color: #504f48;
            line-height: 1.5;
        }

        .feature-dot {
            width: 7px;
            height: 7px;
            border-radius: 50%;
            background: #fbba00;
            flex-shrink: 0;
            margin-top: 6px;
        }

        /* Fallback link */
        .fallback-link-wrap {
            background: #f7f7f5;
            border-radius: 8px;
            padding: 14px 16px;
            margin-top: 20px;
        }

        .fallback-link-label {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #9c9b94;
            margin-bottom: 6px;
        }

        .fallback-link {
            font-size: 12px;
            color: #004e6f;
            word-break: break-all;
        }

        /* Footer */
        .email-footer {
            background: #f7f7f5;
            border-top: 1px solid #eeede9;
            padding: 24px 40px;
            text-align: center;
        }

        .footer-logo {
            font-size: 13px;
            font-weight: 700;
            color: #003750;
            margin-bottom: 6px;
        }

        .footer-address {
            font-size: 12px;
            color: #9c9b94;
            line-height: 1.6;
            margin-bottom: 12px;
        }

        .footer-fine-print {
            font-size: 11px;
            color: #c4c3bd;
            line-height: 1.6;
        }

        @media (max-width: 600px) {
            .email-header, .email-body, .email-footer { padding-left: 24px !important; padding-right: 24px !important; }
            .header-title { font-size: 22px !important; }
            .info-row { flex-direction: column; gap: 2px; }
            .info-value { text-align: left; }
        }
    </style>
</head>
<body>
<div class="email-wrapper">
<div class="email-container">

    {{-- ── Header ── --}}
    <div class="email-header">
        <div class="header-logo-row">
            <div class="header-logo-text">SKIPS University &nbsp;·&nbsp; School of Computer Science</div>
        </div>
        <div class="header-badge">Staff Invitation</div>
        <h1 class="header-title">You're invited to join<br>Academic Tracker</h1>
        <p class="header-subtitle">A colleague has invited you to the SKIPS Academic Activity Tracking platform.</p>
    </div>

    {{-- Gold accent bar --}}
    <div class="accent-bar"></div>

    {{-- ── Body ── --}}
    <div class="email-body">

        <p class="greeting">Hello {{ $invitation->name }},</p>

        <p class="body-text">
            <strong>{{ $invitation->invitedBy->name }}</strong> has invited you to join the
            <strong>SKIPS Academic Activity Tracking System</strong> as
            <strong>{{ $invitation->role->name }}</strong>.
            This platform is used to plan, track, and report academic sessions across all programmes and batches.
        </p>

        {{-- Invitation details card --}}
        <div class="info-card">
            <div class="info-card-title">Your Account Details</div>
            <div class="info-row">
                <span class="info-label">Email</span>
                <span class="info-value">{{ $invitation->email }}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Role</span>
                <span class="info-value">{{ $invitation->role->name }}</span>
            </div>
            @if($invitation->department)
            <div class="info-row">
                <span class="info-label">Department</span>
                <span class="info-value">{{ $invitation->department }}</span>
            </div>
            @endif
            @if($invitation->employee_id)
            <div class="info-row">
                <span class="info-label">Employee ID</span>
                <span class="info-value">{{ $invitation->employee_id }}</span>
            </div>
            @endif
            <div class="info-row">
                <span class="info-label">Invited By</span>
                <span class="info-value">{{ $invitation->invitedBy->name }}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Expires On</span>
                <span class="info-value" style="color:#c0392b;font-weight:700">{{ $invitation->expires_at->format('d M Y, H:i') }}</span>
            </div>
        </div>

        {{-- CTA --}}
        <div class="cta-wrap">
            <a href="{{ route('invitation.accept', $invitation->token) }}" class="cta-btn">
                Accept Invitation &amp; Set Password
            </a>
            <div class="cta-expiry">This link expires in 7 days on {{ $invitation->expires_at->format('d M Y') }}</div>
        </div>

        <div class="divider"></div>

        <p class="body-text" style="font-size:14px;margin-bottom:16px">
            Once you accept, you'll be able to:
        </p>

        <ul class="features-list">
            <li><span class="feature-dot"></span>Log planned and actual sessions for your assigned courses each week</li>
            <li><span class="feature-dot"></span>Track cumulative session targets and variances in real time</li>
            <li><span class="feature-dot"></span>Download and share academic activity reports in Excel format</li>
            <li><span class="feature-dot"></span>Enable two-factor authentication for extra account security</li>
        </ul>

        <div class="divider"></div>

        {{-- Fallback link --}}
        <div class="fallback-link-wrap">
            <div class="fallback-link-label">Button not working? Copy this link</div>
            <a href="{{ route('invitation.accept', $invitation->token) }}" class="fallback-link">
                {{ route('invitation.accept', $invitation->token) }}
            </a>
        </div>

        <p style="font-size:13px;color:#9c9b94;margin-top:20px;line-height:1.6">
            If you were not expecting this invitation, you can safely ignore this email. No account will be created without your action.
        </p>

    </div>

    {{-- ── Footer ── --}}
    <div class="email-footer">
        <div class="footer-logo">SKIPS University</div>
        <div class="footer-address">
            School of Computer Science<br>
            Ahmedabad, Gujarat, India
        </div>
        <div class="footer-fine-print">
            This is an automated message from the SKIPS Academic Activity Tracking System.<br>
            Please do not reply to this email directly.
        </div>
    </div>

</div>
</div>
</body>
</html>